import { Meta, Story, Canvas } from "@storybook/blocks";
import Example from "./Example";
import UserProfile from "../UserProfile";

<Meta title="Pages/UserProfile" component={UserProfile} />

# UserProfile

A `UserProfile` component that displays user information in a styled card. It is responsive and adjusts to different screen sizes.

---

## Example



---

## Usage

To use the `UserProfile` component in your project, import it as follows:

```tsx
import UserProfile from "@gaguirr5/mcxlib/components/userprofile";

const Example = () => {
  return <UserProfile />;
};

export default Example;
